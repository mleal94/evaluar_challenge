const UserController = require('./user');
const PersonController = require('./person');
const ChildController = require('./child');

module.exports = {
  ChildController,
  PersonController,
  UserController,
};
